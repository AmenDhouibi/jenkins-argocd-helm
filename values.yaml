replicaCount: 1

image:
  repository: docker.io/amendhouibi22/springboot-app
  tag: 33
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi
